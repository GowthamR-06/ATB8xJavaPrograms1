package sept.ex_13092024;

public class Lab026_Constant_Or_Final {
    public static void main(String[] args) {
        //constant or final is the value in which value cannot be changed once the values are assigned
        //syntax final datatype variable name(identifier) = variable value(litrals);
        //For constant the variable name are always capital
        //constant are variable
        //Whose values cannot be changed once they are assigned

        final float pie = 3.14234f;

        System.out.println(pie);

    }
}
